# Przykładowa konfiguracja (opcjonalnie)
# Projekt w obecnej formie ma część wartości „na sztywno” w docker/*, ale ten plik
# ułatwia trzymanie ustawień w jednym miejscu.

# PostgreSQL (docker/db)
POSTGRES_DB=cats_db
POSTGRES_USER=docker
POSTGRES_PASSWORD=docker

# Port mapowany na hosta (docker-compose)
APP_PORT=8080
DB_PORT=5433
PGADMIN_PORT=5050

# pgAdmin (docker-compose)
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin
